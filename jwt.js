// Explain JWT in approx and prove it's top 3 benefits.

/**
 JWT stands for JSON Web Token. JWT is a way to exchange data b/w two parties.
    e.g- Two parties are frontend & backend.

 JWT is an open standard for securly transfering data with in parties using a JSON object.

 JWT is used for stateless authentication for users & providers.

 JWT is very secure, where we can sure about integrity of our data.

 Structure of JWT======>
 1. Header
 2. Payload
 3. Signature

 Three Benefits of JWT===========>
 1. Security: JWT provides a secure way to transmit information between parties. The  information is encrypted and can only be decrypted by the intended recipient.

 2. Verification: JWT can be used to verify the identity of the sender. This is possible because each JWT is digitally signed.

 3. Flexibility: JWT is flexible and can be used in a variety of applications.

 */

